$schema: https://railway.app/railway.schema.json

# Railway config
build:
  builder: Dockerfile
  dockerfilePath: ./Dockerfile

deploy:
  startCommand: ""  # Uses Dockerfile CMD
  restartPolicyType: ON_FAILURE
  restartPolicyMaxRetries: 10

$ other:
  PORT: 8080  # Default, overridden by Railway
